plugin: kubernetes.kubevirt.kubevirt
ihost_format: "{name}"
connections:
  - virtctl_proxy: False
    namespaces:
      - virtual-machines
    kubeconfig: /home/runner/.ssh/kubeconfig
#    kubeconfig: kubeconfig
    context: admin
    network_name: vm-net

